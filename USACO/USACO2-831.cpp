#include <bits/stdc++.h>

using namespace std;

int main()
{
    freopen("tttt.in", "r", stdin);
	freopen("tttt.out", "w", stdout);

    int gr = 0, in = 0;
    string f, m, l;
    cin >> f >> m >> l;

    if(f[0] == f[1] && f[1] == f[2])
    {
        in++;
        gr++;
    }
    if(m[0] == m[1] && m[1] == m[2])
    {
        in++;
        gr++;
    }
    if(l[0] == l[1] && l[1] == l[2])
    {
        in++;
        gr++;
    }
    if(f[0] == m[0] && m[0] == l[0])
    {
        in++;
        gr++;
    }
    if(f[1] == m[1] && m[1] == l[1])
    {
        in++;
        gr++;
    }
    if(f[2] == m[2] && m[2] == l[2])
    {
        in++;
        gr++;
    }
    if(f[0] == m[1] && m[1] == l[2])
    {
        in++;
        gr++;
    }
    if(f[2] == m[1] && m[1] == l[0])
    {
        in++;
        gr++;
    }
    if(f[0] == f[1] && f[1] != f[2] || f[0] == f[2] && f[1] != f[2] || f[0] != f[1] && f[1] == f[2])
    {
        gr++;
    }
    if(m[0] == m[1] && m[1] != m[2] || m[0] == m[2] && m[1] != m[2] || m[0] != m[1] && m[1] == m[2])
    {
        gr++;
    }
    if(l[0] == l[1] && l[1] != l[2] || l[0] == l[2] && l[1] != l[2] || l[0] != l[1] && l[1] == l[2])
    {
        gr++;
    }
    if(f[0] == m[0] && m[0] != l[0] || f[0] == l[0] && m[0] != l[0] || f[0] != m[0] && m[0] == l[0])
    {
        gr++;
    }
    if(f[1] == m[1] && m[1] != l[1] || f[1] == l[1] && m[1] != l[1] || f[1] != m[1] && m[1] == l[1])
    {
        gr++;
    }
    if(f[2] == m[2] && m[2] != l[2] || f[2] == l[2] && m[2] != l[2] || f[2] != m[2] && m[2] == l[2])
    {
        gr++;
    }
    if(f[0] == m[1] && m[1] != l[2] || f[0] == l[2] && m[1] != l[2] || f[0] != m[1] && m[1] == l[2])
    {
        gr++;
    }
    if(f[2] == m[1] && m[1] != l[0] || f[2] == l[0] && m[1] != l[0] || f[2] != m[1] && m[1] == l[0])
    {
        gr++;
    }

    cout << in << endl;
    cout << gr << endl;

    return 0;
}
